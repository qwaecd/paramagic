package com.qwaecd.paramagic.tools;


public final class MixinCallback {
    public static void call(int sectionX, int sectionY, int sectionZ, boolean reRenderOnMainThread) {
//        System.out.println("Section changed at (" + sectionX + ", " + sectionY + ", " + sectionZ + "), reRenderOnMainThread: " + reRenderOnMainThread);
    }
}
